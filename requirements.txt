# Core dependencies for quantum_kernel, ai, and llm
numpy>=1.21.0
scipy>=1.7.0

# Deep learning (required by LLM)
torch>=1.9.0

# Optional but recommended for improved embeddings in quantum_kernel
sentence-transformers>=2.2.0

# Optional for better theme extraction in quantum_kernel
scikit-learn>=1.0.0
